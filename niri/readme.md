The KDL files are the configuration files for Niri. They should be placed into ~/.config/niri folder. Niri uses the config.kdl and all the other modules are referenced from there.

There are some other tools used in this config:
- ironbar (for status bar)
- awww (for wallpaper)
